name: Publicar release

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: lilith-release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurar Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Leer versión del proyecto
        id: version
        shell: bash
        run: |
          VERSION="$(go run ./cmd/build version)"
          TAG="v${VERSION#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Publicando Lilith $TAG"

      - name: Verificar que la versión sea nueva
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
        shell: bash
        run: |
          if git rev-parse "$TAG" >/dev/null 2>&1 || gh release view "$TAG" >/dev/null 2>&1; then
            echo "La versión $TAG ya existe. Cambia internal/version/version.go antes de publicar." >&2
            exit 1
          fi

      - name: Validar manifiestos Go
        run: go mod tidy -diff

      - name: Ejecutar pruebas unitarias
        run: go test -mod=readonly -count=1 -timeout=15m -tags=grammar_set_core ./...

      - name: Probar concurrencia del orquestador
        run: go test -mod=readonly -count=1 -race -timeout=10m -tags=grammar_set_core ./internal/agents ./internal/subagents

      - name: Repetir cancelación anidada
        run: go test -mod=readonly -run '^TestCancelParentTearsDownNestedAgentTree$' -count=5 -timeout=2m -tags=grammar_set_core ./internal/subagents

      - name: Ejecutar análisis estático
        run: go vet -mod=readonly -tags=grammar_set_core ./...

      - name: Compilar tests sensibles para Windows
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .tmp/windows-tests
          while read -r package name; do
            GOOS=windows GOARCH=amd64 CGO_ENABLED=0 \
              go test -mod=readonly -tags=grammar_set_core -c \
              -o ".tmp/windows-tests/${name}.test.exe" "$package"
          done <<'PACKAGES'
          ./internal/tools tools
          ./internal/shell shell
          ./internal/subagents subagents
          ./internal/tui tui
          PACKAGES
          rm -rf .tmp/windows-tests

      - name: Verificar integridad de módulos descargados
        run: go mod verify

      - name: Verificar binario estático con gramáticas embebidas
        shell: bash
        run: |
          mkdir -p dist
          CGO_ENABLED=0 go build -tags=grammar_set_core -trimpath -buildvcs=false -o dist/li-static-check ./cmd/li
          if command -v ldd >/dev/null 2>&1 && ldd dist/li-static-check 2>&1 | grep -qvE 'not a dynamic executable|statically linked'; then
            echo "El binario de comprobación tiene dependencias dinámicas inesperadas:" >&2
            ldd dist/li-static-check >&2 || true
            exit 1
          fi
          rm -f dist/li-static-check

      - name: Probar instalador Linux y compilación Termux
        run: |
          sh -n install.sh
          python3 .github/scripts/test_install_sh.py

      - name: Probar selección del instalador PowerShell
        shell: pwsh
        run: ./.github/scripts/test_install_ps1.ps1

      - name: Compilar binarios Linux y Windows
        run: go run ./cmd/build build

      - name: Generar sumas SHA-256
        shell: bash
        run: |
          cd dist
          sha256sum li-* > SHA256SUMS.txt

      - name: Detectar release anterior
        id: previous
        shell: bash
        run: |
          PREVIOUS_TAG="$(git tag --list 'v*' --sort=-v:refname | head -n 1)"
          echo "tag=$PREVIOUS_TAG" >> "$GITHUB_OUTPUT"
          if [ -n "$PREVIOUS_TAG" ]; then
            echo "Generando cambios desde $PREVIOUS_TAG"
          else
            echo "No existe un tag anterior; se documentará la primera versión."
          fi

      - name: Generar notas del release
        env:
          TAG: ${{ steps.version.outputs.tag }}
          PREVIOUS_TAG: ${{ steps.previous.outputs.tag }}
          REPOSITORY: ${{ github.repository }}
        shell: bash
        run: |
          python3 .github/scripts/release_notes.py \
            --tag "$TAG" \
            --previous-tag "$PREVIOUS_TAG" \
            --repository "$REPOSITORY" \
            --output dist/RELEASE_NOTES.md
          cat dist/RELEASE_NOTES.md >> "$GITHUB_STEP_SUMMARY"

      - name: Crear tag y release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
        shell: bash
        run: |
          gh release create "$TAG" dist/* \
            --target "$GITHUB_SHA" \
            --title "Lilith $TAG" \
            --notes-file dist/RELEASE_NOTES.md
